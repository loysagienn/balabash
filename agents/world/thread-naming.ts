// How threads are titled — one clause for every surface that asks a model to
// name a thread (thread_title on spawn functions, spawn_agent, end_thread).
export const THREAD_NAMING_NOTE = 'Name the task itself, never the agent or executor: no "agent: …"-style prefixes.';
